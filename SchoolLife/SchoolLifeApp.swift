import SwiftUI

@main // 👈 "여기가 앱의 시작점이다"라는 뜻입니다!
struct SchoolLifeApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView() // 👈 앱을 켰을 때 처음 보여줄 뷰 이름
        }
    }
}
